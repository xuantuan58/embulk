description = "APIs to access Embulk's core features"
ext {
    summary = "APIs to access Embulk's core features"
}

repositories {
    mavenCentral()
}

dependencies {
    compile "org.msgpack:msgpack-core:0.8.11"
}
